<Window x:Class="Steamoff.App.Views.LanguageSelectionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:models="clr-namespace:Steamoff.Core.Models;assembly=Steamoff.Core"
        xmlns:conv="clr-namespace:Steamoff.App.Converters"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="{Binding Loc[language.dialog.title]}"
        Width="560" Height="460" MinWidth="420" MinHeight="360"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundBrush}"
        FontFamily="Segoe UI">
    <Window.Resources>
        <conv:LanguageEqualityConverter x:Key="LanguageEqualityConverter"/>
    </Window.Resources>
    <Grid Margin="28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Style="{StaticResource HeadingTextStyle}" Text="{Binding Loc[language.dialog.title]}"/>
        <TextBlock Grid.Row="1" Style="{StaticResource SubheadingTextStyle}" Margin="0,6,0,18"
                   Text="{Binding Loc[language.dialog.subtitle]}"/>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Languages}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type models:AppLanguage}">
                        <Border Width="150" Margin="0,0,12,12" Padding="14"
                                Cursor="Hand">
                            <Border.InputBindings>
                                <MouseBinding MouseAction="LeftClick"
                                              Command="{Binding DataContext.SelectCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                              CommandParameter="{Binding}"/>
                            </Border.InputBindings>
                            <Border.Style>
                                <Style TargetType="Border" BasedOn="{StaticResource CardStyle}">
                                    <Style.Triggers>
                                        <DataTrigger Value="True">
                                            <DataTrigger.Binding>
                                                <MultiBinding Converter="{StaticResource LanguageEqualityConverter}">
                                                    <Binding/>
                                                    <Binding Path="DataContext.SelectedLanguage" RelativeSource="{RelativeSource AncestorType=ItemsControl}"/>
                                                </MultiBinding>
                                            </DataTrigger.Binding>
                                            <Setter Property="BorderBrush" Value="{StaticResource AccentOrangeBrush}"/>
                                            <Setter Property="BorderThickness" Value="2"/>
                                            <Setter Property="Background" Value="{StaticResource SurfaceElevatedBrush}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <StackPanel HorizontalAlignment="Center">
                                <TextBlock Text="{Binding FlagEmoji}" FontSize="34" HorizontalAlignment="Center"/>
                                <TextBlock Text="{Binding DisplayCode}" FontWeight="SemiBold" FontSize="15"
                                           HorizontalAlignment="Center" Margin="0,8,0,2"/>
                                <TextBlock Text="{Binding NativeName}" Style="{StaticResource CaptionTextStyle}"
                                           HorizontalAlignment="Center" TextAlignment="Center"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <Border Grid.Row="3" Margin="0,18,0,0" Padding="16" CornerRadius="14"
                Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource AccentOrangeBrush}" BorderThickness="1.5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="{Binding SelectedLanguage.FlagEmoji}" FontSize="22" Margin="0,0,10,0"/>
                    <TextBlock Text="{Binding SelectedLanguage.NativeName}" Style="{StaticResource BodyTextStyle}" VerticalAlignment="Center"/>
                </StackPanel>
                <Button Grid.Column="1" Style="{StaticResource PrimaryButtonStyle}"
                        Content="{Binding Loc[language.dialog.confirm]}"
                        Command="{Binding ConfirmCommand}"/>
            </Grid>
        </Border>
    </Grid>
</Window>
